# Optional: override the API base used to generate banner URLs.
# Useful when running the builder locally but pointing to a deployed API.
# Example:
# VITE_API_BASE=https://github-animatedbanner.vercel.app/api
VITE_API_BASE=
